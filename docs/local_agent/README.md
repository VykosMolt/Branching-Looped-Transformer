# Local-Agent Docs

Local-agent source lives under `../../src/local_agent/`. This directory holds
the wrapper state log and historical snapshots so the source tree stays focused
on runtime modules.

- `PROJECT_STATE_LOCAL_AGENT.md`: current local-agent wrapper state.
- `snapshots/`: preserved snapshots from earlier root/local-agent layouts.
